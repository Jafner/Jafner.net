apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: send-claim0
  name: send-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
