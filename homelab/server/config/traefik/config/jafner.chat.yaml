tcp:
  routers:
    jafner.chat:
      rule: "HostSNI(`jafner.chat`) || HostSNI(`*.jafner.chat`)"
      service: jafner.chat
      tls: 
        passthrough: true
  services:
    jafner.chat:
      loadBalancer:
        servers:
          - address: "192.168.1.24:443"
