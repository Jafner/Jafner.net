version: "3"
services:
  ddclient:
    image: linuxserver/ddclient
    container_name: ddns_ddclient
    restart: "no"
    networks:
      - ddns
    env_file:
      - ddclient.env
    volumes:
      - ./ddclient:/config
    labels:
      - traefik.enable=false

networks:
  ddns: