tcp:
  routers:
    jafner.dev:
      rule: "HostSNIRegexp(`jafner.dev`,`*.jafner.dev`)"
      service: jafner.dev
      tls: 
        passthrough: true
  services:
    jafner.dev:
      loadBalancer:
        servers:
          - address: "192.168.1.24:443"
