http:
  routers:
    monk:
      rule: "Host(`backup.nas.jafner.net`)" 
      entryPoints: "websecure"
      middlewares: 
        - "lan-only@file"
      service: "monk@file"
      tls:
        certResolver: "lets-encrypt"

  services:
    monk:
      loadBalancer:
        servers:
          - url: "http://192.168.1.11/"
      