tcp:
  routers:
    fighter:
      rule: "HostSNIRegexp(`jafner.net`,`*.jafner.net`)"
      service: fighter
      tls: 
        passthrough: true
  services:
    fighter:
      loadBalancer:
        servers:
          - address: "192.168.1.23:443"
