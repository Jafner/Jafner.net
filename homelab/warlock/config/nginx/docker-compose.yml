version: '3.9'
services:
  nginx:
    image: nginx
    container_name: nginx_test
    networks:
      - web
    labels:
      - traefik.http.routers.nginx.rule=Host(`nginx.warlock.jafner.net`)
      - traefik.http.routers.nginx.tls.certresolver=lets-encrypt

networks:
  web:
    external: true
