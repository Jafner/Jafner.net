apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: 5e-jafner-dev
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`5e.jafner.dev`)
    kind: Rule
    services:
    - name: s5etools
      port: 80
  tls:
    certResolver: myresolver