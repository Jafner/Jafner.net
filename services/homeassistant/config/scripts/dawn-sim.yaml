alias: Dawn Simulation (Generic)
sequence:
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 10
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 20
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 30
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 40
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 50
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 60
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 70
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 80
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 90
    action: light.turn_on
  - delay:
      hours: 0
      minutes: 6
      seconds: 0
      milliseconds: 0
  - target:
      entity_id: "{{ dimmer_entity_id }}"
    data:
      brightness_pct: 100
    action: light.turn_on
mode: parallel
icon: mdi:weather-sunset-up
fields:
  dimmer_entity_id:
    selector:
      entity: {}
    name: Dimmer Entity ID
    description: Homeasssitant entity ID of dimmer to act on
    required: true
